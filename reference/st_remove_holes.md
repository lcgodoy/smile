# Remove holes from a `sfc` POLYGON

internal use. Taken from <https://github.com/michaeldorman/nngeo/>

## Usage

``` r
st_remove_holes(x)
```

## Arguments

- x:

  a `sf` or `sfc` polygon.

## Value

a `sf` or `sfc` polygon.
